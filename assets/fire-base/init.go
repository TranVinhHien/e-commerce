package main

func main() {
	// // Use a service account
	// // Replace "path/to/your/serviceAccountKey.json" with the actual path
	// // You might want to store the path in an environment variable or config file
	// opt := option.WithCredentialsFile("path/to/your/serviceAccountKey.json") // Make sure this path is correct!

	// // Initialize the Firebase app with your project ID
	// config := &firebase.Config{ProjectID: "sample-firebase-ai-app-17d13"}
	// // app, err := firebase.NewApp(context.Background(), config, opt)
	// if err != nil {
	// 	log.Fatalf("error initializing app: %v", err)
	// }

	// log.Println("Firebase app initialized successfully!")

	// // We'll get the messaging client in the next step...
	// // ... and send a message after that!
}
